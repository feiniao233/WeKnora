# AGENTS.md

## 智能诊断当前计划

智能诊断后续范围与建议排期以 [Steel 当前建设计划](../../work/steel-platform-v-3/docs/intelligent-diagnosis-current-plan.md) 为唯一入口。
旧计划和归档内容仅供明确的历史追溯；计划状态不能代替代码或验收证据。WeKnora 自身的产品路线图不作为 Steel 智能诊断任务清单。

## 智能助手跨仓库开发

跨仓库接口或职责边界变更时，读取 `../../work/steel-platform-v-3/AGENTS.md` 中的“智能助手跨仓库开发规范”；
准备部署时再读取其中部署说明。其他任务按需读取相关章节，无需完整阅读该文件。

- WeKnora 负责知识库、文档处理、检索和 Agent 能力；修改前先确认现有接口，禁止重复建模。
- 知识库目录只影响管理和筛选，不改变物理文件、向量索引或默认跨目录检索。
- 不将租户、联网、前端等当前未使用能力扩散到 Steel 或 mcp-app。
- 前端实现仅考虑桌面端，不做移动端适配。
- 每项功能独立 commit 并 push；本任务 AI 分支的普通 push 已授权，无需重复确认。默认不创建 MR/PR，不处理无关工作区文件。
- Git 提交信息严格遵守 Conventional Commits，标题格式为 `<类型>(<范围>): <描述>`。
  类型仅限 `feat`、`fix`、`docs`、`style`、`refactor`、`perf`、`test`、`chore`；描述使用中文并以动词开头（如“新增”“修复”“优化”），不超过 50 个字且结尾不加句号。改动较大时在正文补充修改动机，简单改动只保留一行标题。
- Go 变更至少运行相关测试；安全边界变更运行更完整的测试与构建。
